# ADR 0005：AgentTask 采用依赖就绪与等待老化调度

Stella 继续使用本地单执行器，但 `queued` 不再等于可运行：根任务可运行，子任务必须等待父任务进入 `waiting_children`，结构无效的队列项显式失败。就绪项按任务规格快照优先级、每 15 分钟一次的无上限等待老化和持久化数组顺序排序，并在认领事务内重新计算；这避免随机 UUID 决定顺序、子任务抢跑和低优先级任务永久饥饿，同时不为本地 GUI 引入 PostgreSQL 或分布式图调度器。
